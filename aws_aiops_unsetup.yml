---

- name: Get nodes from Inventory and add to Groups
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
  - include_role: 
      name: utils_hosts_aws


- name: Unsetup RHELs with Lightspeed
  hosts: rhel
  become: true
  tasks:
    - debug:
        # var: hostvars[inventory_hostname].tags.Name
        var: inventory_hostname

    - name: Unregister using Org ID and Activation Key
      community.general.redhat_subscription:
        state: absent
        org_id: "{{ org_id }}"
        activationkey: "{{ activation_key }}"
      become: true 

    - name: Unregister the insights client
      redhatinsights.insights.insights_register:
        state: absent
